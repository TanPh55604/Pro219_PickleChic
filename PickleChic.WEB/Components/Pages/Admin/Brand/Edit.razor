@attribute [Route(RouterConfig.Admin.EditBrand)]
@layout AdminLayout

<PageTitle>@Constant.metaDataAdmin Edit Brand</PageTitle>

@if (_isLoading)
{
    <EditPageSkeleton FieldCount="3" />
}
else
{
    <div class="mx-auto w-full max-w-5xl !space-y-6">
        <div class="flex flex-col !gap-4 md:flex-row md:items-center md:justify-between">
            <div>
                <h1 class="text-2xl font-bold text-slate-900">Cập nhật thương hiệu</h1>
            </div>

            <a href="@RouterConfig.BuildRoute(RouterConfig.Admin.BrandDetail, Id)"
               class="inline-flex items-center justify-center !gap-2 !rounded-xl !border !border-slate-200 !bg-white !px-5 !py-2.5 text-sm font-semibold text-slate-700 shadow-sm transition hover:!bg-slate-50">
                <MudIcon Icon="@Icons.Material.Filled.ArrowBack" Size="Size.Small" />
                Quay lại
            </a>
        </div>

        <div class="!rounded-2xl !border !border-slate-200 !bg-white !p-6 shadow-sm md:!p-8">
            <MudForm @ref="_form"
                     Model="@_model"
                     Validation="@_validator.ValidateValue">
                <div class="!space-y-6">
                    <div>
                        <h2 class="text-lg font-bold text-slate-900">Thông tin thương hiệu</h2>
                    </div>

                    <div class="grid !gap-5 md:grid-cols-2">
                        <MudTextField @bind-Value="_model.Name"
                                      For="@(() => _model.Name)"
                                      Label="Tên thương hiệu"
                                      Placeholder="Nhập tên thương hiệu"
                                      Variant="Variant.Outlined"
                                      Required="true"
                                      Immediate="true" />

                        <MudSelect T="int"
                                   @bind-Value="_model.Status"
                                   For="@(() => _model.Status)"
                                   Label="Trạng thái"
                                   Variant="Variant.Outlined">
                            <MudSelectItem T="int" Value="1">Đang hoạt động</MudSelectItem>
                            <MudSelectItem T="int" Value="0">Tạm ẩn</MudSelectItem>
                        </MudSelect>

                        <div class="md:col-span-2">
                            <MudTextField @bind-Value="_model.Description"
                                          For="@(() => _model.Description)"
                                          Label="Mô tả"
                                          Placeholder="Nhập mô tả"
                                          Variant="Variant.Outlined"
                                          Lines="4"
                                          Immediate="true" />
                        </div>
                    </div>

                    <div class="flex justify-end !border-t !border-slate-200 !pt-6">
                        <AuthorizeControl Action="@Constant.ActionButton.Edit">
                            <MudButton Variant="Variant.Filled"
                                       Color="Color.Primary"
                                       StartIcon="@Icons.Material.Filled.Save"
                                       OnClick="SubmitAsync"
                                       Disabled="@_isSubmitting"
                                       Class="!rounded-xl !px-6 !py-2.5 !normal-case">
                                @(_isSubmitting ? "Đang cập nhật.." : "Cập nhật")
                            </MudButton>
                        </AuthorizeControl>
                    </div>
                </div>
            </MudForm>
        </div>
    </div>
}

@code {
    [Parameter] public int Id { get; set; }

    private MudForm? _form;

    private BrandModel _model = new();

    private readonly BrandSchema.CreateUpdate _validator = new();

    private bool _isLoading = true;
    private bool _isSubmitting;

    protected override async Task OnInitializedAsync()
    {
        await LoadBrandAsync();
    }

    private async Task LoadBrandAsync()
    {
        _isLoading = true;

        try
        {
            var result = await _adminBrandService.GetByIdAsync(Id);

            if (!result.Success || result.Data is null)
            {
                _snackbar.Add(ConvertBrandErrorMessage(result.Message), Severity.Error);
                _navigation.NavigateTo(RouterConfig.Admin.Brands, replace: true);
                return;
            }

            _model = MapToModel(result.Data);
        }
        finally
        {
            _isLoading = false;
        }
    }

    private static BrandModel MapToModel(BrandResponse response)
    {
        return new BrandModel
        {
            Id = response.Id,
            Name = response.Name,
            Description = response.Description,
            Status = response.Status
        };
    }

    private async Task SubmitAsync()
    {
        if (_form is null || _isSubmitting)
        {
            return;
        }

        await _form.ValidateAsync();

        if (!_form.IsValid)
        {
            return;
        }

        _isSubmitting = true;

        try
        {
            var result = await _adminBrandService.UpdateAsync(_model);

            if (!result.Success)
            {
                _snackbar.Add(ConvertBrandErrorMessage(result.Message), Severity.Error);
                return;
            }

            _snackbar.Add("Cập nhật thương hiệu thành công", Severity.Success);

            _navigation.NavigateTo(RouterConfig.BuildRoute(RouterConfig.Admin.BrandDetail, Id));
        }
        finally
        {
            _isSubmitting = false;
        }
    }

    private static string ConvertBrandErrorMessage(string message)
    {
        return message switch
        {
            "Không tìm thấy" => "Không tìm thấy thương hiệu",
            "Db Error" => "Không thể xử lý dữ liệu thương hiệu",
            "TokenExpired" => "Phiên đăng nhập đã hết hạn",
            "InvalidToken" => "Phiên đăng nhập không hợp lệ",
            _ => string.IsNullOrWhiteSpace(message)
                ? "Có lỗi xảy ra. Vui lòng thử lại"
                : message
        };
    }
}